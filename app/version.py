# ColdVault version. Bump on every iteration:
#   PATCH (x.y.Z) for fixes/tweaks, MINOR (x.Y.0) for new features,
#   MAJOR (X.0.0) for breaking changes. Add a matching CHANGELOG.md entry.
VERSION = "1.3.0"
